<!DOCTYPE html>
<html>
<head><link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"></head>
<body class="container mt-5">
    <h2><%= item ? 'Edit Barang' : 'Tambah Barang Baru' %></h2>
    <form action="<%= item ? '/items/edit/' + item.id : '/items/create' %>" method="POST">
        <div class="mb-3">
            <label>Nama Barang</label>
            <input type="text" name="item_name" class="form-control" value="<%= item ? item.item_name : '' %>" required>
        </div>
        <div class="mb-3">
            <label>Kategori</label>
            <input type="text" name="category" class="form-control" value="<%= item ? item.category : '' %>" required>
        </div>
        <div class="row">
            <div class="col-md-6 mb-3">
                <label>Harga</label>
                <input type="number" name="price" class="form-control" value="<%= item ? item.price : '' %>" required>
            </div>
            <div class="col-md-6 mb-3">
                <label>Stok Saat Ini</label>
                <input type="number" name="stock_qty" class="form-control" value="<%= item ? item.stock_qty : '' %>" required>
            </div>
        </div>
        <div class="row">
            <div class="col-md-6 mb-3">
                <label>Min. Stok (Peringatan)</label>
                <input type="number" name="min_stock" class="form-control" value="<%= item ? item.min_stock : '' %>" required>
            </div>
            <div class="col-md-6 mb-3">
                <label>Lokasi Rak</label>
                <input type="text" name="storage_location" class="form-control" value="<%= item ? item.storage_location : '' %>" required>
            </div>
        </div>
        <button type="submit" class="btn btn-primary">Simpan</button>
        <a href="/items" class="btn btn-secondary">Batal</a>
    </form>
</body>
</html>